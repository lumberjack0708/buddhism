import styled from '@emotion/styled';

// 購物車頁面樣式

// 購物車項目右對齊容器樣式
export const CartItemRightContainerStyle = {
  textAlign: 'right'
};

// 購物車數量文字
export const CartQuantityText = styled.span`
  margin: 0 8px;
  font-weight: bold;
`; 